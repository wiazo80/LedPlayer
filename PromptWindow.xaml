<Window x:Class="LedPlayer.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Имя композиции"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#1B1B28">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#34344E"/>
            <Setter Property="Foreground" Value="#E8E8F0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#4C4C72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A40"/>
            <Setter Property="Foreground" Value="#E8E8F0"/>
            <Setter Property="BorderBrush" Value="#3A3A55"/>
            <Setter Property="CaretBrush" Value="#E8E8F0"/>
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="18">
        <TextBlock Text="Имя новой композиции:" Foreground="#9A9AB0" FontSize="13"/>
        <TextBox x:Name="InputBox" Margin="0,10,0,0" KeyDown="OnInputKeyDown"/>
        <TextBlock Text="Например: «Утро», «Вечер пятницы», «Акция март»"
                   Foreground="#6E6E88" FontSize="11" Margin="0,8,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сохранить" Width="110" Click="OnOkClick"/>
            <Button Content="Отмена" Width="80" Margin="8,0,0,0" Click="OnCancelClick"/>
        </StackPanel>
    </StackPanel>
</Window>